Usage:

  script/generate paperclip_on_akamai Class attachment1 (attachment2 ...)
  
This will create a migration that will add the proper columns to your class's table. You should have the default paperclip fields allready added on the target table. (See paperclip)